# dedao-gui 开发任务清单

## 任务概述

本清单包含4个核心任务，需要逐一完成并测试验证。所有改动请勿提交/推送，完成一个汇报一个。

---

## 任务一：修复图标加载

### 背景
- 当前应用的图标显示不正确，需要绑定正确的猫头鹰图标
- 图标文件已从原仓库拉取并恢复：
  - `build/favicon.ico` (67,646 bytes)
  - `build/windows/icon.ico` (21,017 bytes)

### 具体要求
1. **检查 wails.json 配置**
   - 确认图标路径是否正确指向恢复的文件
   - 确保编译时能自动识别并绑定图标

2. **检查 main.go**
   - 确认 WindowIcon 配置是否正确引用 icon.ico
   - 如果没有正确引用，需要修复

3. **验证编译输出**
   - 执行 `wails build -platform windows/amd64`
   - 检查生成的 exe 是否带有正确的猫头鹰图标
   - 检查任务栏、桌面快捷方式图标是否正常显示

### 交付物
- [ ] 修改后的配置文件（如有）
- [ ] 截图验证（编译后exe图标+任务栏图标）
- [ ] 无任何提交，纯本地修改

---

## 任务二：内联 login_helper 到主程序

### 背景
- 当前项目有两个独立可执行文件：`dedao-gui.exe` 和 `login_helper.exe`
- login_helper.exe 需要与主程序同目录运行，存在分发风险
- 目标是将登录功能直接集成到主程序中，消除独立进程依赖

### 技术方案（二选一，推荐方案A）

#### 方案A：内联到主程序（推荐）
1. 将 `cmd/login_helper/main.go` 的逻辑合并到 `backend/login_browser.go`
2. 使用 goroutine 在后台启动 WebView 窗口
3. 保持原有的回调服务器逻辑不变
4. 移除对独立 login_helper.exe 的调用

#### 方案B：保留但优化打包
1. 修改 NSIS 脚本，确保 login_helper.exe 自动安装到正确位置
2. 在应用启动时自动检测并复制 login_helper.exe 到临时目录
3. 提供更完善的错误提示

### 技术要求
- 必须保持手机号+验证码登录功能正常工作
- 必须处理好 WebView 的生命周期管理
- 必须保持原有的 Cookie 拦截机制

### 验证步骤
1. 编译主程序
2. 双击打开应用
3. 点击"手机号登录"
4. 观察 WebView 窗口是否正确弹出
5. 输入手机号获取验证码
6. 完成登录后，主程序是否正常接收登录成功事件
7. 测试退出登录后再登录一次

### 交付物
- [ ] 修改后的后端代码
- [ ] 前端无变化（接口不变）
- [ ] 视频或截图验证登录流程
- [ ] 无任何提交

---

## 任务三：修复所有类型文件的下载顺序问题

### 背景
- 当前下载课程PDF时，文件名使用 `OrderNum` 字段作为前缀编号
- `OrderNum` 是搜索API返回的不稳定排序值，不同次搜索可能不同
- **不只是PDF！** MP3、Markdown、EPUB等所有类型的下载都存在同样问题
- 导致下载的文件编号混乱，无法按章节顺序排列

### 问题分析

**当前代码（`backend/app/download.go`）中多处存在：**
```go
name = fmt.Sprintf("%03d.%s", v.OrderNum, name)
```

**问题：**
- `OrderNum` 来自搜索结果，不是真实的章节序号
- 不同关键词、不同时间点的搜索结果，同一课程的 OrderNum 可能完全不同
- 合并搜索结果时没有统一排序
- 影响范围：PDF下载、MP3下载、Markdown下载、EPUB电子书下载

### 解决方案

#### 方案A：使用真实章节序号（推荐）
1. 在所有下载函数中（DownloadPdfCourse, DownloadMarkdown, extOdobDownloadData等）
2. 改为直接调用课程详情API：`CourseInfo(enid)`
3. 从返回的 `ClassInfo` 或章节列表中获取稳定的章节顺序
4. 使用章节的真实序号（第1章、第2章...）作为文件前缀

#### 方案B：使用发布时间排序
1. 从课程API获取所有章节的发布时间
2. 按发布时间正序排列
3. 重新分配序号 001, 002, 003...

### 涉及文件
- `backend/app/download.go` - 主要修改文件
- 需要检查所有调用 `OrderNum` 的地方

### 验证步骤
1. 找一个有多个章节的课程（如"文明之旅"）
2. 分别下载PDF、MP3、电子书
3. 检查下载的文件编号是否按 001, 002, 003... 顺序排列
4. 再次下载同一课程，检查编号是否一致
5. 对比不同搜索结果时的编号是否稳定

### 交付物
- [ ] 修改后的 `backend/app/download.go`
- [ ] 至少3组验证截图（PDF/MP3/电子书各一组）
- [ ] 无任何提交

---

## 任务四：实现并行下载 + 进度条

### 背景
- 原仓库作者 yann0917 已升级到并行下载和实时进度条
- 当前版本是单线程下载，速度慢且用户体验差
- 需要跟上原仓库的升级

### 参考实现（原仓库）
请查看原仓库的相关提交，重点研究：
1. `backend/downloader/downloader.go` - 下载器实现
2. `backend/downloader/types.go` - 数据结构定义
3. `frontend/src/components/DownloadDialog.vue` - 前端进度展示

### 实现要求

#### 后端改造
1. **重构下载管理器**
   - 实现基于 SQLite 的任务队列
   - 支持暂停/继续/取消/重试操作
   - 多工作线程并发下载（建议5-10个并发）

2. **下载状态持久化**
   - 使用 BadgerDB 或 SQLite 存储任务状态
   - 应用重启后自动恢复未完成的任务

3. **进度通知机制**
   - 通过 Wails 事件系统推送进度更新
   - 支持单个文件和批量任务的进度回调

#### 前端改造
1. **进度条UI**
   - 显示当前下载文件
   - 显示总体进度百分比
   - 显示下载速度、剩余时间

2. **任务管理界面**
   - 显示所有下载任务列表
   - 支持单个任务的暂停/继续/删除
   - 显示每个文件的下载进度

3. **批量下载优化**
   - 优化批量下载时的资源调度
   - 避免同时发起过多请求导致被封

### 技术要点
- 必须保持与原 API 兼容
- 错误处理要完善（网络中断、文件冲突等）
- 进度条要流畅不卡顿
- 内存占用要可控

### 验证步骤
1. 下载单个课程，观察进度条是否正常
2. 下载多个课程，观察是否并行执行
3. 暂停某个下载，观察是否能正确恢复
4. 关闭应用后重新打开，检查任务是否恢复
5. 断网后恢复网络，检查是否自动续传

### 交付物
- [ ] 后端下载模块重构代码
- [ ] 前端进度组件
- [ ] 截图验证（进度条+任务列表）
- [ ] 视频演示并行下载效果
- [ ] 无任何提交

---

## 执行优先级

| 优先级 | 任务 |
|--------|------|
| P0 | 任务一：图标修复 |
| P1 | 任务三：下载顺序（所有类型） |
| P1 | 任务二：内联登录 |
| P2 | 任务四：并行下载+进度条 |

---

## 注意事项

1. **严禁提交代码** - 所有任务完成后，只汇报结果，不提交不推送
2. **保留备份** - 修改前务必备份关键文件
3. **单独测试** - 每个任务独立测试，验证无误再开始下一个
4. **记录问题** - 遇到困难及时记录，不要盲目尝试

---

## 联系方式

如有疑问，请联系项目所有者确认需求细节。
